# League Draft Website Dev Container
# Extends the base dev container image
FROM dom/devcontainer:latest

# No additional setup needed - base image has everything
# Add project-specific tools here if needed
